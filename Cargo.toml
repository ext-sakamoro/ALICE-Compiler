[package]
name = "alice-compiler"
version = "1.0.0"
edition = "2021"
authors = ["Moroya Sakamoto"]
license = "MIT OR Apache-2.0"
description = "ALICE DSL/JIT compiler infrastructure: AST, IR, code generation, optimization passes"

[lib]
crate-type = ["rlib"]
